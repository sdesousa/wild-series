<nav class="navbar navbar-expand-lg">
    <a class="navbar-brand" href="/">Wild series</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
            <a class="nav-item nav-link" href="{{ path('wild_index_program') }}">Séries</a>
            <a class="nav-item nav-link" href="{{ path('wild_index_actor') }}">Acteurs</a>
        </div>
    </div>

    {% if app.user %}
        {{ app.user.email }}
        <a class="btn justify-content-end" href="{{ path('app_logout') }}"> Se déconnecter</a>
    {% else %}
        <a class="btn justify-content-end" href="{{ path('app_login') }}"> Se connecter</a>
    {% endif %}


</nav>

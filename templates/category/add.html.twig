{% extends 'base.html.twig' %}

{% block title %}Add a category{% endblock %}

{% block body %}
    <div class="d-flex flex-column p-5">
        <h1>Ajouter une catégorie : </h1>

        <div class="searchField p-3">
            {% form_theme form 'foundation_5_layout.html.twig' %}
            {{ form_start(form) }}
            {{ form_row(form.name) }}
            <button type="submit" name="btnAddCategory" class="align-self-end btn btn-lg">{{ button_label|default('Save') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
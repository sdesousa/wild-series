{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">
        <div class="w-50 mt-5 d-flex flex-column justify-content-center">
            <h1> Mon compte</h1>
            <div>Mon mail : {{ app.user.email }}</div>
            <div>Mes roles : </div>
            <ul>
                {% for role in app.user.roles %}
                    <li class="ml-5">{{ role }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>


{% endblock %}
